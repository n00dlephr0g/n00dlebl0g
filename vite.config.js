import { defineConfig } from "vite";

export default defineConfig({
    // Uncomment to use JSX:
    // esbuild: {
    //   jsx: "transform",
    //   jsxFactory: "m",
    //   jsxFragment: "'['",
    // },
    build: {
        outDir: "../n00dlephr0g.github.io"
    }
});
